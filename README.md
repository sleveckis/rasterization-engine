# Rasterization Render Engine in C++
Engine written in C++ that accepts triangle primitives in an input file and renders them via rasterization. Requires CMake to be installed on the host machine to compile. 
